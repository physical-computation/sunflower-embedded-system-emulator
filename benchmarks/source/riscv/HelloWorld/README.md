# A simple example targeting the RISC-V architecture.
The example uses the simplified `printf` library, hence
```c
#include <printf.h>
```
and the call
```c
	printf_("Hello, World.");
```
Rather than
```c
	printf("Hello, World.");
```
